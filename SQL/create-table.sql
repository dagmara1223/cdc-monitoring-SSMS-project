USE CDC_Project; -- the name of your project

CREATE TABLE fromTable(
	ID INT PRIMARY KEY,
	Name VARCHAR(50),
	Age INT
);

CREATE TABLE toTable(
	ID INT PRIMARY KEY,
	Name VARCHAR(50),
	Age INT
);

